{
  "name": "pg-sql",
  "version": "1.0.0",
  "description": "Node PGSQL Client",
  "main": "index.js",
  "scripts": {
    "postinstall": "for D in */;  do echo \"Installing stuff on ${D}\" && npm install --cwd \"${D}\"; done",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
  },
  "author": "",
  "license": "ISC"
}
