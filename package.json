{
  "name": "pg-sql",
  "version": "1.0.0",
  "description": "Node PGSQL Client",
  "main": "index.js",
  "scripts": {
    "install:pg": "cd "node_modules/pgDB/pg" && npm install",
    "postinstall": "npm run install:pg",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
  },
  "author": "",
  "license": "ISC"
}
