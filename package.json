{
  "name": "pg-sql",
  "version": "1.0.0",
  "description": "Node PGSQL Client",
  "main": "index.js",
  "scripts": {
    "install:pg": "cd pg && npm install",
    "install:pg-connection-string": "cd pg-connection-string && npm install",
    "install:pg-cursor": "cd pg-cursor && npm install",
    "install:pg-pool": "cd pg-pool && npm install",
    "install:pg-protocol": "cd pg-protocol && npm install",
    "install:pg-query-stream": "cd pg-query-stream && npm install",
    "postinstall": "run-p install:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
  },
  "author": "",
  "license": "ISC"
}
